/**
 * Константы модуля Journal: тип журнала, сообщения UI.
 */

/** Значение типа журнала (из URL). */
export const JournalType = {
    STRESS: "1",
    PROGNOZ: "2"
};

/** Сообщения для showDialog. */
export const JournalMessages = {
    DATA_NOT_FOUND: "Данные не найдены"
};
